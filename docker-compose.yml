version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: farmdirect_ai
      POSTGRES_USER: farmdirect
      POSTGRES_PASSWORD: farmdirect
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
